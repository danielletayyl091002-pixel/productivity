import type { Dictionary } from "./dictionary";

export function generateSystemPrompt(_dictionary: Dictionary): string {
  // TODO: Generate a system prompt explaining the CJK compression to an AI
  return "";
}
