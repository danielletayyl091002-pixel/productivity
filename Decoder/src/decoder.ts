import type { Dictionary } from "./dictionary";

export function decode(compressed: string, _dictionary: Dictionary): string {
  // TODO: Implement CJK compressed → English text decoding
  return compressed;
}
